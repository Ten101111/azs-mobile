# CLAUDE.md — Классификатор АЗС ОНПО

Этот файл читается Claude Code автоматически при каждой сессии в этой папке.
Здесь сохранён весь контекст проекта для продолжения работы без повторных объяснений.

---

## Проект

**Название:** Классификатор АЗС — корпоративный мобильный инструмент ЛУКОЙЛ ОНПО
**Назначение:** Мониторинг, классификация и анализ данных АЗС для сотрудников АУП
**Тип:** React 19 PWA + FastAPI + SQLite (текущий стек) → план миграции ниже

---

## Текущий стек (production-ready)

| Слой | Технология |
|------|-----------|
| Frontend | React 19, Vite, PWA |
| Backend | FastAPI + Uvicorn |
| База данных | SQLite (текущая) |
| Аутентификация | Email OTP (одноразовый код на @lukoil.com) |
| Сессии | Cookie `azs_session`, TTL 12 часов |
| Данные | JSON-файлы: `public/stations.json`, `public/staff_recommendations.json` |
| Запуск | `npm start` (python3 scripts/run_app.py --mode pwa) |

### Важные пути
```
backend/main.py          — FastAPI приложение (весь backend)
backend/sql/             — SQL-шаблоны запросов
data/auth.sqlite3        — база аутентификации
data/auth_allowlist.json — разрешённые email-адреса
public/stations.json     — данные АЗС (текущие, из JSON)
src/main.jsx             — React приложение
src/styles.css           — стили
```

### Разрешённые email-домены
`lukoil.com, lukoil.ru, licard.com, spb.lukoil.com, ynp.lukoil.com`

### Аутентификация и восстановление пароля
- Пароль хранится только как PBKDF2-SHA256-хеш — восстановить его нельзя, только сбросить.
- Сброс: экран входа → «Забыли пароль?» → код на корпоративную почту → код + новый пароль.
- Endpoints: `/api/auth/request-password-reset`, `/api/auth/reset-password` (все сессии сбрасываются).

### Аналитика использования (добавлено 19.08.2026)
- Таблицы в `data/auth.sqlite3`: `visits` (визиты: время входа/выхода, длительность, IP, устройство, браузер, PWA) и `usage_events` (просмотры экранов, открытия карточек АЗС, фильтры).
- Frontend шлет heartbeat раз в 60 с при видимой вкладке (`/api/metrics/beat`) и батчи событий (`/api/metrics/events`, sendBeacon при уходе со страницы).
- Визит закрывается после 30 мин неактивности (`ANALYTICS_VISIT_GAP_SECONDS`).
- Раздел «Админ» (плитка на главной, только для `ADMIN_EMAILS`): сводка, визиты по дням, популярность разделов, устройства, таблица пользователей, журнал входов. Endpoints: `/api/admin/users`, `/api/admin/activity?days=N`.

---

## Корпоративный стиль ЛУКОЙЛ ОНПО

Строго соблюдать при любых UI/дизайн задачах:

```css
--red:    #C00000;   /* основной акцент — ЛУКОЙЛ красный */
--ink:    #111111;   /* основной текст */
--mid:    #595959;   /* вторичный текст */
--pale:   #F2F2F2;   /* чётные строки таблиц */
--bpink:  #FBEDEF;   /* розовые callout-блоки */
--border: #D9D9D9;   /* границы */
```

- **Шрифт:** Tahoma (основной), Segoe UI, Arial (fallback)
- **Заголовки:** UPPERCASE, letter-spacing: 0.04em
- **Слайды PPTX:** 1280×720px (13.33×7.5 дюймов), красная полоса сверху 5px
- **Нижний колонтитул:** «LUKOIL ОНПО · Инструмент АУП · Конфиденциально»

### Палитра приложения (PWA) — отдельно от документов

Значения выше относятся к документам и слайдам. В приложении с 21.09.2026
действует фирменный красный из спецификации облика ИИ:

```css
--red: #E31E24;        /* акцент: текст, границы, иконки */
--red-dark: #B8171C;
--red-fill: #E31E24;   /* заливка красных кнопок под белым текстом */
--red-fill-hover: #B8171C;
/* тёмная тема */
--red: #FF4A4F;        /* акцент осветляется ради читаемости */
--red-fill: #E31E24;   /* заливка тёмная: белое на #FF4A4F — 3,3:1, на #E31E24 — 4,69:1 */
```

Правило: `--red` — для того, что читается как цвет (текст, обводка, значок);
`--red-fill` — для того, на чём лежит белый текст. Менять местами нельзя,
иначе в тёмной теме кнопки теряют контраст.

PNG-иконки приложения (`public/icon-*.png`) несут настоящий логотип ЛУКОЙЛ
и к этой палитре не приводятся.

### Системные шкалы (с 21.09.2026)

Любая новая вёрстка берёт значения отсюда, а не подбирает на глаз:

```css
--s1…--s7      /* 4 / 8 / 12 / 16 / 24 / 32 / 48 — отступы кратны 4 */
--r-sm/md/lg   /* 10 / 14 / 20, плюс --r-full для пилюль */
--e1 / --e2    /* уровень 1 — поднятая подложка, 2 — плавающее */
--t-meta…--t-display  /* 12 / 13,5 / 16 / 20 / 44 */
```

Нейтральные — одна шкала тона 220° в обеих темах. Разделение линий:

- `--hairline` — разделители внутри блока;
- `--line` — то же, общее;
- `--line-mid` — декоративное: пунктиры, соединители, дорожки ползунка;
- `--line-strong` — **границы полей формы**, 3,29:1 к белому (WCAG 1.4.11).
  Не использовать как декоративную линию: она специально тёмная.

Правило поверхностей: контент лежит на фоне, разделяют его воздух и уровень.
Обводка остаётся только у того, что действительно поднято, — боковая панель,
поле ввода, модальное окно. `--icon-muted` — только для значков (порог 3:1);
текст красится `--muted` и темнее.

---

## Артефакты презентации

**HTML-презентация (6 слайдов):**
`/private/tmp/.../scratchpad/azs-slides.html`
Артефакт: `https://claude.ai/code/artifact/dad60f78-af67-47e6-88be-b4b4e52118d3`

**PPTX-файл:**
`АЗС_Классификатор_Презентация.pptx` (в корне проекта)
Скрипт генерации: `/private/tmp/.../scratchpad/make_pptx.py`

**ТЗ (техническое задание) v3.0:**
`/private/tmp/.../scratchpad/tz-azs.html`
Артефакт: `https://claude.ai/code/artifact/089da399-dc3c-479d-99cf-85cab5af613c`
Также сохранён как: `ТЗ_Классификатор_АЗС_v3.md` (в корне проекта)

### Критический баг scale-to-fit (РЕШЁН)
Scale JS для слайдов должен использовать `document.body.offsetWidth - 32`, а НЕ `parentElement.offsetWidth`. Иначе скрытые слайды (display:none) получают width=0 и схлопываются. `scale()` вызывать ПОСЛЕ показа слайда в функции `go()`.

---

## Целевая архитектура (план развития v3.0)

### Источник данных
- **DWH PostgreSQL** — только чтение (read-only user)
- Обновляется **каждый час**
- Документация схемы DWH **отсутствует** — нужно создавать с нуля

### Целевой стек
```
React 19 PWA
    ↓
FastAPI + Uvicorn
    ↓
Redis (кеш сессий + результатов, TTL 30 мин)
    ↓
PostgreSQL App DB + PgBouncer (connection pooling)
    ↓
ETL (APScheduler, каждый час в :15)
    ↓
DWH PostgreSQL (read-only)
```

### Text-to-SQL архитектура (Этап 4)
```
Пользователь → текстовый запрос
    ↓
FastAPI /ai/query
    ↓
Ollama (localhost:11434) + Qwen2.5-Coder-14B-Instruct
(system-prompt = YAML-схема DWH из Этапа 1)
    ↓
SQL (только SELECT, проверка через sqlparse)
    ↓
PostgreSQL App DB → результат → пользователь
```

**ВАЖНО:** Никаких внешних API для ИИ. Только локальные модели через Ollama.
Требование безопасности компании — данные не должны покидать инфраструктуру.

---

## Роли пользователей (RBAC)

| Роль | Доступ |
|------|--------|
| Директор НПО | Все АЗС своего НПО |
| Региональный менеджер | АЗС своего региона |
| Менеджер по качеству | Все АЗС (только чтение KPI) |
| Оператор АЗС | Только своя АЗС |

Реализация: JWT-токен с ролью + PostgreSQL Row-Level Security (RLS) как второй рубеж.

---

## Масштаб

- **Пилот:** 10–15 пользователей
- **Production:** до 1 000 пользователей
- **Команда разработки:** 1 full-stack разработчик

---

## Управление пользователями

**Текущий план:** ручная admin-панель (Вариант А)
- Администратор создаёт аккаунты вручную
- Назначает роль и НПО/регион
- Блокирует при увольнении
- Подходит до ~200 человек

**Будущий план при росте:** интеграция с Active Directory через LDAP (Вариант Б)
- Уточнить у IT: есть ли AD в ЛУКОЙЛ и можно ли подключиться

---

## GPU-сервер для локального ИИ (не куплен)

| Вариант | Стоимость | Статус |
|---------|-----------|--------|
| Workstation RTX 4090 24GB (рекомендуется) | ~350–550 тыс. ₽ | Нужно заложить в бюджет |
| Workstation RTX 3090 24GB б/у (пилот) | ~280–380 тыс. ₽ | Дешевле, для тестирования |
| Selectel/Яндекс Cloud GPU (аренда) | ~15–45 тыс. ₽/мес | Требует согласования с ИБ |

Для пилота (10–15 чел.) пока можно использовать CPU-инференс: Qwen2.5-Coder-7B Q2_K (~5 tok/s, ответ 15–30 сек).

---

## Дорожная карта (5 этапов)

### Этап 0: Инфраструктура (1–2 нед.)
- Docker Compose: Nginx + FastAPI + PostgreSQL + Redis + Ollama
- Миграция SQLite → PostgreSQL + PgBouncer
- Redis для сессий и кеша
- Read-only подключение к DWH

### Этап 1: Описание данных DWH (3–5 нед.) — КРИТИЧЕСКИЙ ФУНДАМЕНТ
1. Автовыгрузка структуры через `information_schema` → YAML-черновик
2. EDA по каждой таблице (COUNT, MAX/MIN, LIMIT 20)
3. Смысловое описание каждой таблицы и колонки вручную
4. ER-диаграмма связей (dbdiagram.io или draw.io)
5. «Контракт данных» для ETL (маппинг DWH → App)
6. Валидация с владельцем данных

Результат: YAML-файл 30–100 страниц. Используется ETL (Этап 2) и ИИ (Этап 4).

### Этап 2: ETL (2–3 нед.)
- APScheduler: инкрементальный ETL каждый час в :15
- Стратегия: `WHERE updated_at > последний_запуск`
- Полная перезагрузка ночью (раз в сутки)
- Materialized views для дашбордов (REFRESH CONCURRENTLY)
- Таблица `etl_log`, эндпоинт `/api/data-health`
- Плашка «Данные актуальны на: X минут назад»

### Этап 3: RBAC + Алерты + Пилот (3–4 нед.)
- Admin-панель: создание/редактирование/блокировка пользователей
- JWT с ролями + PostgreSQL RLS
- Push-алерты (Web Push API + VAPID + SMTP)
- Экспорт Excel (openpyxl) и PDF (WeasyPrint)
- Запуск пилота (10–15 человек), сбор обратной связи

### Этап 4: Локальный ИИ Text-to-SQL (6–8 нед.)
- Ollama + Qwen2.5-Coder-14B-Instruct Q4 (10–12GB VRAM)
- YAML-схема из Этапа 1 → system-prompt
- POST /ai/query → LLM → SQL → проверка → результат
- Redis Queue для 1 000 пользователей (очередь)
- Чат-интерфейс с кнопками «👍 Верно» / «👎 Неверно»

---

## Открытые вопросы

1. Есть ли в ЛУКОЙЛ Active Directory (AD)? → уточнить у IT
2. Разрешает ли ИБ облачный GPU (Selectel/Яндекс)? → согласовать
3. Есть ли в DWH поле `updated_at` в ключевых таблицах? → проверить при подключении
4. Кто владелец данных АЗС в DWH, кто может валидировать описание? → найти аналитика/DWH-инженера

---

## Команды разработки

```bash
# Запуск приложения (полный стек)
npm start

# Только frontend
npm run dev

# Только backend
npm run backend

# Сборка PWA
npm run build

# Подготовка данных
npm run prepare-data
npm run prepare-staff
```

---

## Что НЕ делать

- **НЕ** использовать внешние AI API (OpenAI, Anthropic, Yandex GPT и др.) — требование ИБ
- **НЕ** хранить DWH-пароли в коде — только в `.env`
- **НЕ** давать пользователям прямой доступ к DWH — только через ETL-слой
- **НЕ** выполнять INSERT/UPDATE/DELETE в DWH — только SELECT через read-only user

---

## Промежуточные итоги по разделу ИИ (срез на 22.09.2026)

Что сделано и принято владельцем за 21–22.09.2026. Подробности — в разделах
ниже и в «Реестр_открытых_вопросов_БТ.md» (З-15…З-25).

**Готово и в приложении**

| Что | Где | Решение |
|---|---|---|
| Раздел «ИИ-аналитик»: история диалогов, лента, строка рассуждения с настоящими этапами, оценка звёздами, поток этапов (SSE) | `src/main.jsx`, `backend/ai/` | З-15, З-18, З-20 |
| Слой токенов: шкалы отступов, радиусов, уровней, типографики; одна нейтральная гамма; красный акцент отделён от заливки кнопок | `src/styles.css` | З-17, З-21 |
| Раздел «Качество ответов» в админке (три вкладки: аналитика, пользователи, качество) | `AiQualityPanel`, `backend/ai/quality.py` | З-19 |
| Приведение типов на границе исполнителя SQL (Decimal, даты, NaN, большие целые) | `backend/ai/executor.py` | З-24 |
| Крупный орб на WebGL — порт «Recursive Erosion» в корпоративных цветах; вшит в пустой экран и блок ожидания, момент «готово» — орб передачи в ответе | `src/orb/` | З-16, З-25 |
| Мелкие значки: прежний графитовый знак в строке разбора и на боковой панели; в отказах и ошибках значка нет; кнопка «ИИ-аналитик» везде как на главной | `AiMark`, `viewItems` | решения 22.09.2026 |
| Телефон: поле ввода 85 px, три подсказки, кнопка истории в шапке, «Контроль» убран с нижней панели, подписи панели не усекаются на 360–430 px | `src/styles.css` | З-23 |
| Аналитический агент: разбор задачи, глубины auto/fast/analyze/deep, инструменты (схема по каталогу, SQL через прежний валидатор с проверками качества, Python-песочница, графики), структурированный ответ, память диалога, сверка чисел | `backend/ai/agent/`, `backend/ai/semantic.py`, `src/aiAnalysis.jsx` | З-27 |
| РУ, ТМ на дату и текущие статус и тип АЗС из справочников ОХД `bds` — модель видит только четыре поля и ключи, остальное отсекает валидатор | `build_dwh_catalog.py`, `validator.py`, `check_dwh_people.py` | З-28 |

**Решено владельцем, менять только новым решением**

- Орб — точь-в-точь референс с 21st.dev (частицы), не SDF-кольцо и не «улучшенная» версия; фон прозрачный везде.
- Кнопка «ИИ-аналитик» в навигации нейтральная, как на главной (без красного акцента и без орба).
- В блоках ленты орб не используется: там прежний графитовый знак; в отказе и ошибке — только заголовок.
- Оценка до трёх звёзд требует комментария; SQL виден только администратору и субадминистратору; диалог виден только автору.
- Надпись про БТ-КК10 остаётся в интерфейсе — подписью под лентой качества, не блоком.

**Открыто**

- 60 к/с орба на телефоне — проверка владельцем (стенд рендерит программно, цифры оттуда не показатель). Ручки: `quality: "low"`, `maxDpr`.
- Три белых столбца — якорь спецификации З-16 — в референсе отсутствуют и в крупный орб не добавлены; остались только в мелком знаке. Вопрос владельцу.
- Область субадминистратора в «Качестве ответов» и классификатор вопросов (З-22).
- Стенд проверяет типы витрины только тестами на подменённом исполнителе; эталонный набор идёт по SQLite (З-24).

---

## Аналитический агент (с 22.09.2026)

Контур «вопрос → SQL → ответ» стал одним из режимов (FAST). Поверх него —
агент: разбор задачи, план, серия инструментов, структурированный ответ.
Полное описание — «Архитектура_ИИ_аналитика_агент.md»; здесь то, что нужно
помнить при правках.

**Где что лежит**

- `backend/ai/pipeline.py` — точка входа `ask(question, role, binding, actor, model, on_stage, depth, history)`.
  `depth`: `auto | fast | analyze | deep`. `fast` без истории — прежний путь без единого
  изменения (`_ask_fast`). Иначе разбор задачи (`agent.loop.triage`): эвристика
  `memory.looks_like_lookup` отправляет очевидный факт-вопрос на быстрый путь без
  обращения к модели, остальное — одна JSON-подсказка модели. `MODEL_FACTORY` —
  точка подмены модели в тестах и сценарном прогоне.
- `backend/ai/semantic.py` — семантический слой: показатели (ключ, формула, единица,
  направление, драйверы), измерения, сущность «АЗС», правила периодов, группы
  сопоставимых и список **того, чего в витрине нет**. Два встроенных профиля — стенд
  SQLite и витрина ОХД; секция `semantic` файла каталога дополняет и переопределяет
  формулы без правки кода. Формулы модель берёт отсюда, а не придумывает.
  Поиск по слою (`find`, правка 24.09.2026): названия сравниваются целыми словами —
  двухбуквенные сокращения («УС», «КС», «РУ», «ТМ», «КЛ», «ВД», «МП», «АБ», «ДТ»)
  никогда не ищутся внутри слов, служебные слова — `_SHORT_STOP`; общие слова
  («сколько», «количество», «АЗС», «объект» — `_GENERIC`) показатель не выбирают,
  название только из них совпадает лишь целой фразой. Совпали все слова названия —
  0,85 и +0,03 за каждое следующее слово (до двух), целой фразой — ещё +0,05; часть
  слов — 0,5 × доля. К этому — редкость слов вопроса: +0,1 × Σ 1/n (n — у скольких
  показателей это слово), не больше 0,15. Синоним добавляй как фразу пользователя
  («график плана», «средний чек») и проверяй `test_ai_semantic_search` — там основной
  (48 вопросов, первым — все) и отложенный (25, первым — не меньше 21) наборы.
- `backend/ai/agent/` — `loop.py` (оркестратор), `tools.py` (реестр и проверка аргументов),
  `schema_tools.py` (схема по каталогу, не по information_schema), `sql_tool.py` (run_sql
  через прежний валидатор и исполнитель + `checks.py`), `sandbox.py` (run_python в
  подпроцессе, белый список импортов, без окружения и файлов) + `analytics.py`
  (примитивы: тренд, выбросы, разложение LMDI, парето, what-if, прогноз А/Б по методике),
  `charts.py` (create_chart: значения берутся из результатов, не из текста модели),
  `memory.py` (рамки диалога, эвристики), `grounding.py` (сверка чисел ответа с данными),
  `prompts.py`, `llm.py` (Ollama с tool calls / JSON, `ScriptedModel`), `state.py`.
- `backend/ai/agent_demo.py` — прогон шести вопросов владельца: `--scripted` без модели
  (проверяет контур), без флага — живая Ollama. Запуск через `scripts/python.sh`.
- `src/aiAnalysis.jsx` — ответ агента: структура, графики на SVG (line, bar,
  stacked_bar, scatter, waterfall, kpi, table), таблицы, «Показать расчёты».
  `main.jsx` подключает его в `AiAnswerBody` (когда у ответа есть `analysis`) и в
  `aiStages` (этапы из `answer.steps`). Переключатель глубины — в настройках раздела.

**Правила, которые нельзя ослаблять**

1. Каждый SQL агента идёт через тот же `validator.validate` с областью данных и LIMIT;
   у агента отдельный лимит строк (`AI_AGENT_ROWS`, `AI_AGENT_DEEP_ROWS`), не снимать.
2. Модель видит только каталог. Инструменты схемы читают каталог и семантический слой;
   `information_schema` не используется намеренно (п. 5 задания отклонён владельцем в
   пользу принципа allowlist). Новая таблица появляется правкой каталога, не инициативой модели.
3. Числа в тексте ответа сверяются с результатами (`grounding`): одна попытка исправления,
   затем неподтверждённые фразы убираются, факт пишется в трассу. Пороги из SQL и кода
   считаются законными свидетельствами.
4. Пустой результат — не ноль: и быстрый путь (`_no_data_note`), и агент (`checks`) говорят
   «данных нет» с доступным диапазоном дат. Показатель из списка `absent` не досчитывается —
   попадает в «Ограничения данных».
5. Python-код и SQL шагов уходят на клиент только admin/subadmin (`_strip_code` в api);
   текст шагов (purpose) — всем. История диалога в разбор берётся только из диалога автора.
6. Песочница — защита от ошибок модели, не граница безопасности: подпроцесс, лимиты CPU/
   времени/вывода, белый список импортов, без переменных окружения. Не давать ей сеть и базу.
7. Бюджеты глубины (`state.Budget`): analyze — 5 SQL / 2 Python / 2 графика / 10 ходов;
   deep — 10 / 4 / 3 / 18; общий предел `AI_AGENT_MAX_SECONDS` (240 с). Ollama:
   `AI_NUM_CTX` (32768 с 25.09.2026, прежде 16384) — меньше 16384 нельзя, серия результатов не поместится.
   С 23.09.2026 тот же `num_ctx` передают и быстрый путь, и пояснение (`generator.NUM_CTX`,
   переменная `AI_NUM_CTX` или прежняя `AI_AGENT_NUM_CTX`): при разном контексте Ollama
   перезагружала модель на каждом переключении «Лёгкий» ↔ агент. Ответ Ollama с ошибкой
   (HTTP 500 и т. п.) теперь несёт её текст и подсказку — «Internal Server Error» без причины
   больше не показывается. Случай 23.09.2026: ход агента упёрся в `num_predict` 1500
   (рекомендации по шаблону длинные, ~16 токенов/с), Ollama ответила 500. Теперь поля
   рекомендации — короткие фразы, модель пишет 4 поля из 6, рекомендаций 1–2; сорвавшийся
   ход после сбора данных не роняет ответ — итог пишет `_finalize` (`AI_AGENT_FINAL_TOKENS`, 1500).

**Формат ответа агента (API)**: к прежним полям добавлены `depth`, `taskType`,
`analysis {headline, happened[], why[], where[], actions[], limitations[]}`, `charts[]`
(`{id, type, title, x[], series[{name, values[]}], unit, source}`), `tables[]`, `steps[]`,
`frame` (рамка для следующего вопроса), `plan`, `grounding`, `planMs`. Всё это лежит в
`answer_json` сообщения диалога и в `trace_json` журнала.

**Проверено**: 106 тестов (`backend/tests`, из них 35 новых в `test_ai_agent.py`);
сценарный прогон шести вопросов на полном стенде — 6 из 6, числа подтверждены;
стенд интерфейса — десктоп и телефон, без ошибок и горизонтальной прокрутки.
**Не проверено**: живая модель qwen3 через Ollama из среды разработки недоступна —
первый живой прогон делает владелец (`scripts/python.sh -m backend.ai.agent_demo`).
Ожидаемые слабые места 8B-модели: JSON разбора задачи (есть запасной план), выбор
инструментов (есть подталкивание и принудительный финал), длина контекста.

## Раздел «ИИ-аналитик» в приложении

Собран 21.09.2026 по прототипу, согласованному с владельцем.

- **Фронтенд:** `src/main.jsx`, блок от `function AiMark` до конца
  `AnalyticsAiConsole`; стили — блок «Раздел «ИИ-аналитик»» в `src/styles.css`.
  Боковая панель с историей, лента колонкой по центру (760px), строка
  рассуждения, свёрнутые блоки, окно оценки звёздами, выдвижная история
  на телефоне.
- **Бэкенд:** `backend/ai/dialogs.py` — диалоги, сообщения и оценки в той же
  базе, что и журнал обращений (`JOURNAL_DB`), связь через `journal_id`.
  Маршруты — в `backend/ai/api.py`.
- **Три правила, которые нельзя ослаблять** (проверяются тестами
  `backend/tests/test_ai_dialogs.py` и `test_ai_api.py`):
  1. Диалог виден только своему автору, включая администратора. Администратор
     работает со сводкой оценок (`GET /api/ai/quality`), а не с перепиской.
  2. Оценка до трёх звёзд включительно требует комментария — на сервере,
     а не только в интерфейсе.
  3. SQL уходит на клиент только администратору и субадминистратору
     (`_may_see_sql`). Прятать его вёрсткой недостаточно: он был бы виден
     в трафике.
- **Этапы в строке рассуждения строятся только из измеренных величин**
  (`model_ms`, `narrate_ms`, `sql_ms`, `attempts`, `row_count`). Не добавлять
  этап, время которого никто не засекал.
- **Движение.** Ровно три анимации на framer-motion: вход сообщения (8 px +
  прозрачность, 220 мс), пружинное раскрытие свёрнутого блока (`AiReveal`),
  поочерёдное проявление этапов (40 мс на этап). Больше в ленту не добавлять.
  Въезжает только то, что пришло в этом сеансе (`freshIds`), иначе открытие
  старого диалога выглядит как поток новых сообщений. Всё уважает
  `useReducedMotion`.
- **Поток этапов.** `POST /api/ai/ask/stream` отдаёт события `stage`, затем
  `answer` либо `failed`. Конвейер сообщает об этапах через `on_stage`;
  слушатель не должен ронять ответ, поэтому исключение в нём гасится.
  Клиент читает обычным `fetch` (не `EventSource`: тот умеет только GET,
  а вопрос в адресной строке попадёт в логи прокси) и при неудаче молча
  повторяет через `/ask`.
  **Не включать gzip на этом пути** — компрессор копит байты, и этапы
  приедут пачкой в конце. За это отвечает `StreamFriendlyGZip` в
  `backend/main.py`; TestClient буферизует тело, поэтому разброс во времени
  проверяется только прогоном через настоящий сервер.
- **Раскладка раздела.** Высота считается раскладкой, а не подобранным
  числом: `.app-shell.ai-mode .workspace` — колонка, `.view-stage` и
  `.analytics-pane` сжимаются, лента листается внутри себя. Не возвращать
  `calc(100dvh - N)`: при другой высоте шапки поле вопроса уезжает за край.
  Нижнюю кромку можно тянуть (`--ai-extra`, запоминается в localStorage),
  на телефоне кромки нет.
- **Подсказки.** Нажатие кладёт вопрос в поле ввода, а не отправляет:
  период или объекты почти всегда хочется поправить. Три штуки, не больше:
  четвёртая не помещается.
- **Телефон.** Поле ввода — 85 px: текст и кнопка отправки в одну строку,
  подпись-подсказка под полем скрыта. Всё, что отнимаешь у поля, достаётся
  ленте, а она на телефоне и так короткая. По всей цепочке
  `.ai-shell → … → .ai-table-card` стоит `min-width: 0`: у flex-элемента
  по умолчанию `min-width: auto`, и одна широкая таблица внутри ответа
  растягивала весь раздел за правый край экрана. Не убирать.
- **Кнопка истории диалогов живёт в шапке приложения**, а не в заголовке
  раздела: на телефоне это единственное место, где она не отнимает высоту
  у ленты. Состояние панели (`aiDrawer`) поэтому поднято в `App`, а консоль
  принимает его пропсами `drawer` / `onDrawer`.

- **Пустой экран.** Там стоит крупный орб (`AiOrb`), размер — переменная
  `--ai-orb-size` на `.ai-welcome` (240 px десктоп / 144 телефон, коридоры по
  высоте окна; ниже 760 px десктоп опускается до 168–190 px — иначе поле
  вопроса уезжает за край, а спрятанный ввод хуже уменьшенного орба). Ярлык
  на кнопке-подсказке короткий, модели уходит полная формулировка с периодом
  (`AI_EXAMPLES` — пары `hint`/`ask`). Размеры задаются переменными, а не
  width/height в разметке: инлайновый стиль перебивает правило. У блока
  `justify-content: safe center` — обычное `center` при нехватке места
  срезает **верх**.

## Знак и орб ИИ-аналитика: что где стоит (актуально на 22.09.2026)

Три разных вещи, их легко перепутать:

| Компонент | Что это | Где стоит сейчас |
|---|---|---|
| `AiMark` (вариант plain, по умолчанию) | SVG: графитовый круг #1A1D23, три белых столбца, без красного контура. Работа — пульс столбцов, отказ и ошибка — приглушённые столбцы | Строка разбора у готового ответа («Рассуждал / Разбирал вопрос»), боковая панель (подпись 17px) |
| `AiMark variant="orb"` | SVG-орб по спецификации З-16: красный контур, внутренний свет, восемь состояний | **Нигде.** Сохранён как компонент; ветка `mark` в навигации спит |
| `AiOrb` → `src/orb/orbEngine.js` | WebGL-частицы, порт «Recursive Erosion» | Пустой экран раздела (240/144 px), блок ожидания в ленте (120/96 px), орб передачи «готово» в пришедшем ответе |

Шапка отказа, уточнения и ошибки — **без значка**, только заголовок.
Кнопка «ИИ-аналитик» в верхнем ряду и на нижней панели — Sparkles, как на
главной, без акцента и без орба.

### Крупный орб — порт «Recursive Erosion»

`src/orb/`: `orbEngine.js` (движок без зависимостей), `AiOrb.jsx`
(React-обёртка на Framer Motion), `demo.html`, `README.md`. Решение владельца
от 22.09.2026: орб — **точь-в-точь** «Recursive Erosion» (Meng To, ThreeUI,
MIT; 21st.dev/@mengto), только в корпоративных цветах. Предыдущая SDF-версия
лежит рядом как `*.sdf.legacy.js` — не подключена, хранится для истории.

- **Что это.** Не Three.js и не SDF, а частицы на WebGL1: 2500 точек на сфере
  Фибоначчи, симплекс-шум мнёт сферу, второй шум с порогом выедает дыры
  (эрозия), семь «червей» ползут по большим кругам и подсвечивают точки,
  третий проход рисует их хвосты жемчужинами со свечением, поверх — зерно
  вторым холстом. Исходник взят из registry 21st.dev и портирован дословно:
  геометрия, шум, черви, проходы, константы. **Не «улучшать» эти числа** —
  владелец просил ровно референс.
- **Что изменено, в шейдере помечено `LUKOIL`:** палитра (оранжевая лестница
  cDim→cMid→cHot стала красной), убран бейдж-подпись, пять констант стали
  пружинными параметрами состояний — порог эрозии `thr`, амплитуда смятия
  `morph`, сила трасс `worms`, скорость петли `speed`, яркость `light`; плюс
  `mute` для отказа и ошибки (уход в металл #8A8D92).
- **Фон прозрачный везде** (решение владельца). Красный по светимости вдвое
  темнее оранжевого, поэтому у движка `mode: dark | light | auto`, как у
  оригинального компонента: на тёмном ступени палитры подняты (иначе решётка
  в покое исчезает), на светлом — опущены, «горячий» край упирается в чистый
  #E31E24, зерно не рисуется. `AiOrb` передаёт `mode="auto"`.
- **Размер точек.** Оригинал рассчитан на мастер 1080 px; на 120–240 px точки
  вырождались в пыль. Ниже 400 px они растут в полтора раза с полом 1,5 px.
- **Reduced motion** — как в оригинале: петля стоит на кадре 1,2 с, смена
  состояния перерисовывает кадр один раз.
- **Состояния** — цели параметров, к которым каждый идёт своей пружиной;
  переходы перекрываются сами. Шесть из брифа (idle, listening, searching,
  analyzing, forming, ready) плюс refused и error для исходов приложения.
  `orbStateFromPipeline`: набор текста → listening, draft и check → searching,
  read → analyzing, write → forming, исход → ready / refused / error.

### Как орб вшит в ленту

- **Пустой экран** — `idle`; `listening`, пока курсор в поле или набран текст;
  клик по орбу ставит курсор в поле. Размер — `--ai-orb-size` на `.ai-welcome`.
- **Блок ожидания** (`AiThinking pending`) — орб над строкой этапов.
- **Момент «готово» показывает `AiSettleOrb`** в пришедшем ответе: блок
  ожидания уходит из дерева в тот же кадр, в который приходит ответ, сказать
  ему «готово» некому. Орб передачи монтируется на том же месте (вопрос и
  ответ свёрстаны одинаково), стартует с параметров «формирую»
  (`initialState`), без появления (`appear={false}`), переходит в исход
  и через 1,4 с сворачивается. Ответ не задерживается ни на кадр. Колбэк
  `onSettled` — через ref: родитель перерисовывается на каждое нажатие
  клавиши, и таймер иначе сбрасывался бы.
- В ленте никогда не живёт больше одного WebGL-холста; после ответа крупных
  холстов в DOM нет. Не ставить `AiOrb` в повторяющиеся элементы.
- Без WebGL `AiOrb` рендерит `fallback` — в обоих местах туда передан `AiMark`.

### Тема

Приложение атрибут `data-theme` не ставит — тема только системная. Правила
для тёмной темы пишутся в `@media (prefers-color-scheme: dark)`; селектор
`:root:not([data-theme="light"])` применялся бы всегда (так и было
21–22.09.2026, исправлено).

### Что осталось от SDF-версии и зачем это помнить

Уроки, которые пригодятся, если SVG-орб (`variant="orb"`) снова понадобится:
в SVG нет `<defs>` и фильтров (им нужны id, а знак повторяется); свет —
отдельный слой `::before`, а не фон корпуса; яркость мелкого знака — множитель
`--orb-light`, а не перезапись `--orb-core-op`; толщина контура `--orb-stroke`
в единицах viewBox и на мелких размерах её надо поднимать; «сбившийся слой»
в ошибке — только дуга, повёрнутый круг неотличим от неповёрнутого.


### Малый орб (с 23.09.2026)

- `src/orb/miniOrb.js` + `AiMiniOrb.jsx` — холст 2D, без WebGL. Идея взята
  у «Thinking Orbs» (разное движение на каждое состояние и отдельная
  настройка под размер), код и облик — свои: точки в цветах токенов
  `--red`, `--text`, `--icon-muted`, тема меняется вместе с приложением.
- Состояния: `idle`, `understand`, `check`, `compute`, `write`, `done`,
  `refused`, `error`. Первые четыре рабочих — это публичные этапы ответа:
  кольцо с бегущим фокусом → полоса проверки → три столбца знака AiMark →
  три строки текста. Точки перетекают между фигурами, а не переключаются.
- Пресеты `MINI_PRESETS`: 20 px — 12 точек, плоское кольцо, столбцы и строки
  разнесены шире (иначе сливаются в пятно); 64 px — 72 точки на сфере.
  Другие размеры берут ближайший пресет.
- Где стоит: в строке этапа блока ожидания (20 px, вместо трёх точек) и
  вместо большого орба, когда нет WebGL (64 px). На панели, в шапке ответа и
  в навигации остаётся тихий знак `AiMark` plain — решение владельца от
  22.09.2026 не менялось.
- Большой WebGL-орб в ожидании берёт состояние от публичного этапа
  (`AI_ORB_STATES`: listening → searching → analyzing → forming), а на каждый
  законченный внутренний шаг даёт импульс `orb.beat()` — слабее вспышки
  «готово». Малый орб делает то же через проп `beat`.
- Витрина состояний: `/src/orb/mini-demo.html` на dev-сервере.

## Бэклог развития ИИ-ассистента и справок (с 23.09.2026)

- Постановки 25 задач ИИ-01…ИИ-25 — `Бэклог_развития_ИИ-ассистента.md`
  (цель, сценарий, что есть в коде, решение, критерии готовности, риски,
  зависимости, приоритет, объём, волна); статусы — лист «Развитие
  ИИ-ассистента» в `Бэклог_проекта_Мобильный_ИИ-аналитик_2026-09-22.xlsx`;
  в реестре — З-30. Нумерация повторяет пункты запроса владельца, в котором
  номер 20 встречается дважды: уровни глубины — ИИ-20, агенты — ИИ-21, дальше
  сдвиг на один; ИИ-25 (разметка «факт / расчёт / гипотеза / рекомендация»)
  добавлен при разборе.
- Любая новая функция ИИ соблюдает принципы П-1…П-8 документа: модель не ходит
  в БД напрямую; SQL — только через `validate()`, в том числе из отложенных
  задач, презентаций и агентов; область данных подставляет код, память папки,
  файлы и агенты её только сужают; утверждения размечены по типам; база
  знаний пополняется только через модерацию; рекомендации — не обязательные
  решения; только локальные модели; всё в журнале.
- Перед правкой индикатора генерации, этапов, поля ввода или ширины ответа —
  открыть карточки ИИ-05, ИИ-06, ИИ-14, ИИ-15: там гипотезы регрессии
  статичных точек и сопоставление внутренних шагов с четырьмя публичными
  этапами. Облик орба (З-16) в этих задачах не меняется.
- Решения владельца Р-1…Р-16 приняты 23.09.2026 — раздел 14 документа и лист
  «Решения владельца» общего бэклога; отложены только Р-1 (внутреннее имя) и
  Р-4 (изображения). Отличия от рекомендаций, уже внесённые в карточки:
  уведомления отложенных задач — только в приложении, без почты (ИИ-24);
  в пилоте четыре агента (ИИ-21, P2); справка публикуется при ≥ 95 % АЗС
  с полными данными, зона внимания — падение топлива на 20 %+ (СП-02, СП-03).
  Лимиты из таблиц ИИ-02 и ИИ-03 утверждены.
- Строки общего бэклога, относившиеся к классификатору (2, 4, 5, 7, 8, 10,
  27), 23.09.2026 сняты после отката; добавлены 49 (проверка справочников ОХД),
  50 (З-29), 51 (этот бэклог) и 52 (справки).
- **Сделано 23.09.2026 (ждёт проверки на телефоне и коммита): ИИ-05, ИИ-13,
  ИИ-14, ИИ-15.** Что важно помнить при правках:
  - «Статичные круги» — это спиннер текущего этапа `.ai-stage-spin`: в стилях
    не было кадров `@keyframes aiSpin` с коммита 9417ccb. Кадры добавлены
    рядом со спиннером; не удалять при чистке «неиспользуемых» кадров.
    Проверка: `el.getAnimations({ subtree: true })` в браузере, а не имя
    анимации в computed style — оно есть и без кадров.
  - При «Уменьшить движение» индикатор ожидания не замирает: вместо вращения
    и смещений — медленная прозрачность (`aiBreathe` 2,4 с), таймер и
    неподвижная фигура малого орба с мерцанием яркости.
  - Ожидание ответа — компонент `AiPending`: одна строка «малый орб этапа ·
    название этапа · таймер справа» и «Подробнее» с внутренними шагами.
    Полосу из четырёх отрезков и счётчик «N из 4» владелец снял 23.09.2026
    как перегрузку — не возвращать. Четыре публичных этапа
    `AI_PUBLIC_STAGES`, сопоставление внутренних шагов — только в
    `aiPublicStageIndex` (новый вид шага агента добавлять туда), этап держится
    не меньше `AI_STAGE_MIN_MS` = 300 мс, после 60 с — подсказка.
  - Поле вопроса: `rows={1}`, высоту считает `aiFitComposer` по `lineHeight`
    (до трёх строк, пустое поле — всегда одна строка), `maxLength` 500 =
    `AskRequest.question`. На телефоне (`pointer: coarse`) Enter переносит
    строку, отправка — кнопкой. Подсказка про Enter и счётчик — в
    `.ai-composer-meta` под полем.
  - История диалогов сворачивается в полосу 56 px (`AiSideRail`,
    `.ai-shell.side-collapsed`), Ctrl/Cmd+B по `event.code === "KeyB"`,
    состояние — `localStorage["azs:ai-side-collapsed"]` в try/catch.
  - Уровни глубины (ИИ-20): подписи «Авто / Лёгкий / Средний / Высокий»,
    коды прежние (`auto/fast/analyze/deep`). Переключатель `AiDepthPicker` —
    в поле вопроса, выбор в `localStorage["azs:ai-depth"]`; из «Настроек» убран.
    Описания и ориентир времени — `api.depth_options()`: медиана `total_ms`
    за 7 дней из журнала при ≥ 5 ответах, иначе предел. `pipeline.ask` —
    обёртка над `_ask`: пишет `total_ms` и `depth_requested`, ответ несёт
    `totalMs` и `depthRequested`. В строке «Рассуждал» — уровень ответа
    (при «Авто» — с причиной), под ответом — «Углубить» (fast → analyze → deep).
  - Приветствие под орбом (ИИ-08, 24.09.2026): `aiWelcomeTitle` и `aiWelcomeScope`
    в `src/main.jsx` — шаблон без модели. Имя — `aiFirstName` (ФИО, «Имя Фамилия»,
    «Фамилия Имя»; инициалы и почта — без обращения). Число объектов —
    `ownStations` / `ownUnrestricted` из `/api/ai/status` (`_own_scope` в
    `backend/ai/api.py`, та же область, что для ответов); ошибка области — строка
    без числа, экран не ломается. При выборе роли «от имени» — её число объектов.
  - Примеры вопросов под орбом и над полем — по роли: `backend/ai/examples.py`
    (`examples`, у администратора ещё `examplesByRole` для вопросов «от имени»);
    `AI_EXAMPLES` в `main.jsx` — только запасной набор. Правила примера: показатель
    есть в семантическом слое витрины (у примера ключ `metric`, его сверяет
    `test_ai_examples`), ничего из «чего в витрине нет», без месяцев и годов,
    ограниченная роль — без сети и чужих обществ, РУ сравнивает только ТМ.
  - Ширина ответа (ИИ-06): текст, таблицы, графики и поле ввода стоят в одной
    колонке `--ai-column`. На десктопе (≥ 861 px) `.ai-main` —
    `container-type: inline-size`, колонка `clamp(720px, 80cqw, 1080px)`
    (883 px при окне 1440, 1080 px на 1920); «Широкий режим» из «Настроек» —
    `min(100cqw - 48px, 1400px)`. За колонку ничего не выходит: широкая
    таблица прокручивается внутри карточки, кнопка ⤢ открывает таблицу или
    график на весь экран (`AiFullscreen`, портал в body, Esc, фокус
    возвращается). На телефоне колонка — вся ширина экрана.
  - Выгрузка ответа (ИИ-12, 24.09.2026): ссылки «Excel» и «CSV» (`AiDownload`
    в `src/aiAnalysis.jsx`) у таблиц и графиков ответа ведут на
    `GET /api/ai/messages/{id}/export?part=main|table-<id>|chart-<id>&format=xlsx|csv`.
    Файл собирает сервер из сохранённого ответа (`backend/ai/export.py`:
    `part_of` → `passport` → `build_xlsx` / `build_csv`), не из браузера.
    Только свой ответ (`dialogs.message` → 404 для чужого); каждая выгрузка —
    строка в `ai_exports`. XLSX: листы «Данные» и «Паспорт», гриф
    «Конфиденциально» в паспорте и колонтитулах; CSV — UTF-8 с BOM, «;»,
    десятичная запятая, паспорт под таблицей. SQL в паспорте и поле `frame.sql`
    в ответе — только admin/subadmin (`_may_see_sql`). Тесты — test_ai_export.
  - Ролевые лимиты сложности (ИИ-03, 24.09.2026): `backend/ai/quotas.py` —
    таблица Р-2 (`DEFAULTS`, роль → группа admin/network/npo/ru, незнакомая роль —
    ru), учёт в `ai_runs` и `ai_alerts` (база журнала), `RUNS` — идущие вопросы
    процесса: не больше 2 одновременных, квота «Высокого» на день МСК, общая
    очередь тяжёлых задач (`AI_HEAVY_SLOTS`=2, `AI_QUEUE_WAIT`=600 с, админ первым),
    отмена. Конвейер получает `quotas.Control`: `enter_deep` (место в очереди или
    понижение до analyze с пометкой), `cancelled` (агент останавливается перед
    следующим ходом, `rule=cancelled`, в диалог не пишется). Пределы уровня —
    `limits.for_run(role, depth)`: 60/120/240 с и 200/1000/2000 строк (РУ deep 1000),
    у admin — ADMIN без пределов. Квоты — по учётной записи спрашивающего
    (`_own_role`), время и строки — по действующей роли. API: отказ 429/400 с
    заголовками `X-AI-Limit`, `X-AI-Suggest-Depth`; поток начинается событием
    `run` (runId); `POST /api/ai/runs/{id}/cancel`; `GET /api/ai/limits`; статус и
    ответ несут `limits`/`quota`; «Качество ИИ» — `load` (нагрузка за 7 дней).
    Журнал `ai_queries`: tokens_in/out, model_calls, tool_calls, stop_reason,
    depth_requested (`journal.set_run_stats`, токены — `generator.USAGE`).
    Правка значений без кода — вкладка «Лимиты ИИ» (ИИ-26). Тесты — test_ai_quotas.
  - «Лимиты ИИ» в админке (ИИ-26, 24.09.2026): `backend/ai/limit_settings.py` —
    описание параметров с диапазонами (`role_params`, `general_params`), правки в
    `ai_limits`, журнал в `ai_limits_log`, `save` проверяет пакет целиком
    (`Invalid`), `reset`, `hits(days)` — упоры за неделю из `ai_runs`, `ai_alerts`,
    `ai_queries` (stop_reason, truncated). quotas читает правки через кэш
    `CACHE_SECONDS`=60 (`quotas._stored`, `quotas.refresh()` после сохранения);
    `set_overrides` — только для тестов, поверх базы. Потолки: AI_AGENT_MAX_SECONDS,
    `executor.MAX_ROWS` (AI_MAX_ROWS), AI_HEAVY_SLOTS_MAX. API (require_admin):
    `GET/PUT /api/ai/admin/limits`, `POST /api/ai/admin/limits/reset`. Алерт №12 —
    `quotas.alert_sink` ← `build_router(..., notify)` ← `notify_ai_alert` в main.py
    (получатель — общая настройка alert_recipient, пусто — ADMIN_EMAILS).
    Фронт — `AiLimitsPanel`. Тесты — test_ai_limit_settings.
  - Статус раздела не ждёт ОХД (24.09.2026): без VPN три запроса «от имени» к витрине
    давали 30 с на каждый `/api/ai/status`, и всё это время «ИИ-аналитик» и вкладки ИИ
    в админке не появлялись. Теперь `dwh_scope.identities(wait=False)` и
    `dwh_scope.cached_build` берут данные из кэша, а при его отсутствии считают в фоне;
    неудача витрины кэшируется на AI_SCOPE_FAIL_TTL (120 с), три запроса идут
    параллельно. Статус несёт `identitiesPending`, роли «от имени» интерфейс
    дозагружает из `GET /api/ai/identities`; приветствие без числа, пока область считается.
  - Сроки хранения и папки (ИИ-02 + ИИ-10, 24.09.2026). Лимиты — в `quotas.DEFAULTS`
    (history_days, active_dialogs, pinned_dialogs, folders, dialog_memory; общее
    audit_days) и на вкладке «Лимиты ИИ». `backend/ai/dialogs.py`: таблица `ai_folders`,
    поздние колонки диалога `folder_id`, `archived_at`, `owner_role` (роль владельца
    обновляет `list_dialogs(role=...)`); лимиты бросают `dialogs.Limit(param, message,
    action)` → в API 409 с заголовками `X-AI-Limit`, `X-AI-Action` (action
    `archive_oldest`). Архив: не считается в активных, снимает закрепление, новый
    вопрос (`append_message`) возвращает из архива. Удаление папки —
    `DELETE /api/ai/folders/{id}?dialogs=move|delete`. Названия папок сравниваются
    через casefold в Python (NOCASE в SQLite не знает кириллицу). Очистка —
    `backend/ai/retention.py`: поток в процессе API (`start_scheduler` из startup,
    `AI_RETENTION=0` — выкл.), раз в час, если с прошлой очистки ≥ 20 ч; удаляет
    незакреплённые диалоги старше срока роли владельца (роль неизвестна — самый
    долгий срок) и строки ai_queries/ai_feedback/ai_exports/ai_runs старше audit_days;
    итог — в `ai_retention_runs`. «Качество ИИ» берёт вопрос из журнала
    (`COALESCE(m.question, q.question)`), поэтому оценки живут дольше диалога.
    `storage_report` → поле `storage` в `/api/ai/admin/limits` (блок «Хранение
    истории»). Память диалога — `_history` берёт dialog_memory прошлых ходов.
    `_persist` создаёт диалог с `check=False` (лимит уже проверен до модели в
    `_can_start`). Фронт: `AiSidebar` (закреплённые → «Папки» → «Без папки» → «Архив»,
    плашка лимита над списком), `AiFolderBlock`, `AiDialogRow` («Переместить в…»,
    перетаскивание на компьютере), `AiStorageReport`. Меню «…» диалога и папки —
    `AiPopMenu`: portal в body с position: fixed (z-index 70–71, выше `.ai-drawer`),
    открывается под кнопкой или над ней, если снизу не хватает места, — иначе
    прокрутка `.ai-sidebar-list` обрезала меню у нижних строк. Тесты — test_ai_folders.

## Обратная связь и режим данных (24.09.2026, общий бэклог №30 и №31)

- Замечания из «Контроля» — `backend/feedback.py`: `POST /api/feedback` (пользователь),
  `GET /api/admin/feedback`, `PATCH /api/admin/feedback/{id}` (администратор). Таблица
  `station_feedback` в auth.sqlite3; статусы new / in_progress / done / rejected, комментарий,
  handled_by. Старые записи из localStorage `azs:feedback` фронт досылает один раз с
  clientId (уникален на пользователя). Предел — 30 замечаний в час от человека.
  Фронт: `ControlDashboard` / `FeedbackCard` (статус только по ответу сервера),
  вкладка админки «Обратная связь» — `FeedbackPanel`. Тесты — test_feedback.
- Режим данных: `data_mode()` по умолчанию `local`; mock только явным APP_DATA_MODE=mock.
  На рабочем сервере (`APP_STAGE=production`) `kpi_mode()` отвечает 503, `/api/health`
  — degraded и `mockBlocked`. При mock фронт (`useDataMode` по `/api/health`) показывает
  плашку «Тестовые данные» над всеми разделами. Тесты — test_data_mode.
  - Плашка `Note` (`src/note.jsx`, образец владельца 24.09.2026): type
    default/secondary/success/warning/error, fill, size small/medium/large,
    label (true — значок, строка — «Лимит:»), action справа (`NoteAction`),
    `as="span"` внутри кнопок. Стили — раздел «Note» в конце styles.css, цвета —
    токены (тёмная тема сама). В разделе ИИ ей показываются лимиты, очередь,
    остановка, ошибки; в «Качестве ИИ» — алерты порога вопросов.
  - Типы утверждений (ИИ-25) и рекомендации (ИИ-16), 23.09.2026. Тип пункта
    ставит код по происхождению чисел, а не модель (`backend/ai/agent/claims.py`,
    поверх `grounding.Locator`): «Факт» — ячейка или число строк результата
    запроса, «Расчёт» — разность, темп, сумма, вывод Python, столбец-отклонение
    или порог из запроса (с формулой из исходных чисел), «Гипотеза» — пункты
    «почему», причинные формулировки и пункты без привязки к данным.
    Рекомендации — `recommend.py`: шесть полей Р-9 (действие, основание, эффект,
    ограничения пишет модель; источник и уверенность ставит код), неполная или
    без опоры на данные/методику не показывается, стоп-лист и закрытые темы —
    `backend/ai/recommendation_rules.json` (черновик, утверждает владелец):
    акции и скидки не предлагаются, оценка и санкции к работникам (К13) —
    тоже. Решение владельца 23.09.2026: рекомендации только по просьбе
    (`recommend.requested`) или когда модель считает их необходимыми — тогда
    не больше одной и с уверенностью не ниже средней. Интерфейс: в самом ответе
    меток нет — пункты обычным списком, у рекомендаций только действие и строка
    «справочный характер» (владелец: метки у каждой строки перегружают экран).
    Тип пункта, источники, формулы и поля рекомендаций (с уверенностью) — в нижнем
    блоке «Расшифровка ответа», он свёрнут. Раскрывающиеся пункты и метки у строк
    владелец отклонил — не возвращать.
    Будущее: перечень действий по событиям в базе знаний — ИИ-27.
  - Единый стиль ответа (23.09.2026, `backend/ai/textstyle.py`): имена колонок и
    показателей латиницей → русское название из семантики и описания каталога
    (`vd_per_client` → «ВД на клиента»), в скобках — убираются; «(сентябрь 2026)» →
    «в сентябре 2026»; число в скобках в конце фразы → «— 71,46 %»; десятичная запятая,
    «11,49 %», типографский минус. Заголовки результатов SQL переименовываются так же
    (`rename_columns` в `sql_tool` и быстром пути), модель дальше видит русские имена.
    Заголовок ответа — всегда одна-две фразы (`textstyle.headline`). Оборванный на
    лимите токенов JSON итога собирается `llm.salvage_json`; сырой JSON на экран не
    попадает никогда (`loop._analysis_from_prose`). Правка текста идёт после сверки
    чисел и разметки типов — числа не меняются, меняется запись.
  - Пределы по ролям — `backend/ai/limits.py`. Решение владельца 23.09.2026: у
    администратора ограничений в рамках лимитов нет — бюджет шагов 40 SQL / 15 Python /
    8 графиков / 60 ходов (страховка от зацикливания), общего предела времени нет,
    10 000 строк, ход модели 4096 токенов, ожидание модели 15 мин, запрос к витрине
    и расчёт — по 120 с (`AI_ADMIN_*`). Защита данных — область, валидатор, только
    чтение, песочница — остаётся для всех. Роль берётся действующая: при просмотре
    «от имени» другой роли действуют её пределы. Лимиты ИИ-02/ИИ-03, когда появятся,
    на администратора не распространяются.
  - Скорость ответа (25.09.2026). Замер по журналу: у администратора «Средний» вырос со
    127 с (3–6 шагов) до 434 с (20 шагов, 19 вызовов модели). Три правки:
    1) решение владельца — на «Среднем» у администратора обычный бюджет шагов
       (`limits.for_run`: 5 SQL / 2 Python / 2 графика / 10 ходов; время и строки — без
       пределов; «Высокий» — без ограничений; `AI_ADMIN_ANALYZE_UNLIMITED=1` — как было);
    2) кэш контекста Ollama: Ollama не перечитывает начало переписки, если оно не
       изменилось. Прежний `loop._trim` каждый ход сжимал ещё один старый результат в
       середине — начало менялось на каждом шаге. Теперь переписка только дописывается,
       а сжатие — разом (все результаты, кроме двух последних), когда переписка
       подходит к `context_limit_chars` = (NUM_CTX × `AI_CONTEXT_TRIM_SHARE` 0,75 − ответ
       хода) × `AI_CHARS_PER_TOKEN` 2,5 знака; число сжатий — `AgentOutcome.context_trims`.
       `OllamaChat._prepare` ставит «/no_think» во все сообщения человека, а не только в
       последнее (метка «переезжала» и тоже меняла начало). `AI_NUM_CTX` по умолчанию 32768
       (также в `reports/publish.py`, чтобы не перезагружать модель); `AI_KEEP_ALIVE`
       (24h) — модель держится в памяти, а не выгружается через 5 минут;
    3) журнал: `generator._count_usage` суммирует prompt_eval/eval/load_duration →
       `ai_queries.model_prompt_ms`, `model_eval_ms`, `model_load_ms`, по вызовам — в
       `model_trace` (JSON: calls, trims). Сводка — `journal.model_timings`, в «Качестве ИИ»
       → «Нагрузка за 7 дней» → «Время модели на ответ». Тесты — test_ai_context_cache.
       Важно: `prompt_eval_count` (tokens_in) у Ollama — весь контекст вызова, вместе с
       кэшем; сколько чтения было на самом деле, показывает только `model_prompt_ms`.
    Итог после перезапуска (25.09.2026, тот же вопрос, «Средний»): 434–542 с → 141 с;
    12 вызовов модели вместо 19–23; модель 123 с — чтение 38 с, письмо 83 с, загрузка 1,6 с;
    кэш работает: контекст 7,5 тыс. токенов читается за ~0,5 с. Остановка — по лимиту
    ходов: 8 из 12 ходов модель потратила на справки по схеме (`get_metric_definition`
    и т. п.) до первого запроса — следующий резерв скорости.
    Настройки Ollama, которые меняет владелец на маке (не код): `OLLAMA_FLASH_ATTENTION=1`,
    при нехватке памяти `OLLAMA_KV_CACHE_TYPE=q8_0`; `ollama ps` должен показывать 100% GPU.
  - «Журнал ИИ» в админке (25.09.2026): вкладка «Журнал ИИ» (`AiJournalPanel`, `AiJournalDetail`)
    — все обращения из `ai_queries`: период, роль, уровень, исход, поиск по тексту (casefold —
    SQLite LIKE не знает кириллицу) и пять тематических фильтров. Бэкенд: `backend/ai/topics.py`
    (разбор), `backend/ai/journal_view.py` (список, карточка, Excel), API (require_admin):
    `GET /api/ai/admin/journal?days&role&depth&outcome&q&topic=facet:значение…&limit&offset`,
    `GET /api/ai/admin/journal/export`, `GET /api/ai/admin/journal/{id}`. Фильтры: theme
    «Тематика», metric «Показатель», slice «Разрез», task «Тип задачи», period «Период»;
    внутри фильтра — «или», между — «и»; счётчик значения считается без выбора своего фильтра.
    Правила (решение владельца: правила + модель для нового): показатели — фразы названий и
    синонимов всех слоёв семантики (рабочий, DWH, стенд) в вопросе с учётом окончаний, длинная
    фраза поглощает вложенную, синоним из одних общих слов («на АЗС») не считается; нет в
    тексте — первые показатели плана агента. Тематика — семейство показателя (`THEMES`);
    показатель вне семейств — своя тематика; без показателей — `KEYWORD_THEMES`. Не узнано —
    «Не определена» и `ai_topic_state.model_state = pending`: фоновый поток `start_worker`
    (из startup при AI_DEMO_ENABLED; `AI_TOPICS=0` — выкл., `AI_TOPICS_MODEL=0` — без модели,
    `AI_TOPICS_INTERVAL` 120 с) раз в интервал раскладывает новые вопросы правилами, а модель
    берёт до 2 вопросов, только если `quotas.RUNS.busy()` ложно. Модель выбирает тему из
    списка или заводит новую (`clean_topic`: без цифр, до 40 знаков; сверх `AI_TOPICS_MAX` 40
    новых — «Прочее»); 3 неудачи — failed. Таблицы: `ai_topics` (реестр, first_seen → метка
    «новая» за 7 дней, origin rules|model), `ai_query_topics`, `ai_topic_state`. `RULES_REV` —
    версия правил: при смене всё раскладывается заново, темы модели сохраняются. Retention
    удаляет темы вместе с записью журнала. Excel — без SQL и шагов. Тесты — test_ai_journal_view.
  - Файлы в вопросе (ИИ-07) и память папки (ИИ-11), 25.09.2026, решения Р-5 и Р-6:
    1) разбор — `backend/ai/file_parse.py`: `detect` сверяет расширение с сигнатурой
       (%PDF-, OOXML-архив с нужными частями, текст без NUL); отказ (`Rejected`) — xls/doc/ppt,
       xlsm/docm/pptm/xlsb, `vbaProject.bin`, OLE-контейнер (пароль), зашифрованный PDF,
       PDF без текстового слоя (скан, OCR нет), zip-бомба (распаковка > 200 МБ или сжатие > 120).
       Лимиты: 20 МБ, 300 страниц PDF, 20 листов, 20 000 строк, 200 колонок. Числа из текста
       превращаются в числа только в CSV и таблицах DOCX; колонки-идентификаторы (АЗС, КССС,
       код, ИНН, телефон) и «0123» остаются текстом. `parse_isolated` — отдельный процесс
       `python -I` без окружения, RLIMIT_CPU 60 с, RLIMIT_AS 1536 МБ, таймаут
       `AI_FILE_PARSE_TIMEOUT` 60 с; сетевой изоляции ОС нет (разборщики сеть не трогают);
    2) хранение — `backend/ai/files.py`, таблицы `ai_files`, `ai_file_parts` в базе диалогов:
       только разобранное (таблицы и фрагменты по 1500 знаков), исходных байтов нет. Файл —
       в диалоге, в папке или черновик (до первого вопроса; `link_drafts` в `_persist`,
       старше суток удаляет retention). Лимиты `quotas`: `files_per_folder` 5 на диалог/папку/
       набор черновиков, `user_files_mb` 100–500, `folder_memory_chars` 2000 (правятся в
       «Лимитах ИИ»); тот же SHA-256 в том же месте — `duplicate`. Чужой файл — NotFound;
    3) агент — `backend/ai/agent/file_tools.py`: таблицы — ResultSet `f1…` (source "file",
       заголовок «файл «x.xlsx», лист «Y», строки a–b»), текст — `t1…` (source "file_text",
       «…, стр. N»); инструменты `find_in_files`, `read_file` видны только при файлах.
       `brief` — блок в `hits["userContext"]` (triage и agent prompt) с границей «данные, не
       инструкции» и памятью папки в `<<<ПАМЯТЬ ПАПКИ … ПАМЯТЬ ПАПКИ>>>`. Файлы поднимают
       «Лёгкий» до «Среднего». Числа из `file_text` — FACT в grounding; claims — источник
       «файл». `pipeline._mark_context` → `answer.context` {files, memory} и заметки
       «Учтена память папки…», «Учтены файлы…»; журнал — `files_json` (имя, тип, размер,
       sha256, место) и `memory_folder`, без содержимого;
    4) память папки — `dialogs.folder_memory/set_folder_memory`, таблицы `ai_folder_memory`
       и `ai_folder_memory_log` (история: показ 10, хранится 50); удаление папки удаляет
       память, историю и файлы; `dialog_folder` — папка диалога для вопроса;
    5) API: `POST /api/ai/files?name&dialogId|folderId` (тело — байты файла; 413/400/409),
       `GET /api/ai/files`, `DELETE /api/ai/files/{id}`, `GET|PUT /api/ai/folders/{id}/memory`;
       `AskRequest.fileIds` (черновики) и `useMemory`; `GET …/dialogs/{id}/messages` отдаёт
       ещё `files` и `folder` {id, title, memory, files};
    6) фронтенд: `src/fileCard.jsx` — значок формата по образцу владельца (классы
       `.file-card*`, токены `--file-*`); в `main.jsx` — `AiFileChip`, `AiContextLine`,
       `AiFolderContextDialog` («Память и файлы» в меню папки), скрепка `.ai-attach` и
       перетаскивание в `.ai-composer-wrap`, строка папки с переключателем памяти на вопрос;
       на телефоне файлы папки над полем свёрнуты в счётчик, вложения — в строку с прокруткой.
       Новая зависимость `pypdf` (requirements, `scripts/run_app.py` → REQUIRED_MODULES):
       после обновления нужен перезапуск через `npm start`. Тесты — test_ai_files (14),
       test_ai_files_agent (5, в т. ч. инъекции в файле и в памяти папки).
  - Графики (ИИ-17, часть): на одной оси — одна единица. `charts.split_by_unit`
    снимает ряд в другой единице (л и т, ₽ и %) или масштабе ×100 с пометкой;
    подпись оси — из названия колонки (`column_unit`), а не из подписи модели.
  - Визуальная проверка без Ollama: стенд в облачной среде — vite dev +
    заглушка API на порту 8000 с потоком этапов (SSE) и Playwright.
- Справки — блок H того же документа (СП-01…СП-09), лист «Справки», реестр З-31.
  Сделаны СП-01…СП-07 — раздел «Справки по сети» ниже. Справка строится только по
  витрине ОХД и собирается на маке владельца (решение 24.09.2026). Простои топлива
  в справку не входят (решение владельца 23.09.2026: их данные пока не актуальны).

## Справки по сети (СП-01…СП-07, с 23.09.2026)

**Главное правило (решение владельца 24.09.2026): справка строится только по
витрине ОХД** — `dm.data_for_ai_analytic_part_1` (факты по АЗС за день) и
`dm.data_for_ai_analytic_part_2` (планы по АЗС за день). База KPI платформы
(`station_kpi_daily`) и стенд для справок не годятся: `weekly.Source` отказывается
строить выпуск по любому другому каталогу, сервер не принимает модель, у которой в
паспорте источник не «Витрина ОХД». Витрина видна только с мака владельца под VPN,
поэтому **справку собирает мак, а сервер только принимает и показывает готовый
выпуск**. Серверный таймер сборки по базе KPI снят (deploy.sh его отключает).

- Пакет `backend/reports/`:
  - `periods.py` — неделя пн–вс, прошлая неделя, та же неделя прошлого года (−364
    дня), 8 недель динамики, месяцы недели (`months_of`, переходная неделя),
    праздники РФ (только отметка в паспорте).
  - `weekly.py` — модель выпуска `build(week, …)`. Цифры — только плитки ОХД из
    `summary.TILES` (без `STAND_TILES`) через `validate()`; топливо — в тоннах;
    в «Цифрах недели» есть ВД НТУ. Значения — по всей сети, изменения — по
    сопоставимой базе (7/7 дней в обоих периодах), доли — в п. п. Пороги Р-15 —
    `THRESHOLDS` (env `REPORT_*`). При смене формул поднимать `FORMULAS_VERSION`.
  - План месяца (СП-07, `_plan`): план месяца — сумма дневных планов part_2;
    выполнение — по объектам, у которых есть план на месяц; % прошедших дней,
    отставание (% плана − % дней), требуемый темп в день и текущий темп; по сети и
    по ОНПО; статус по выручке НТУ: до 3 п. п. — норма, 3–8 — внимание, больше 8 —
    критично (статус — только по НТУ). Переходная неделя: закрытый месяц —
    «Итоги месяца», новый — план на дату. План загружен не на все дни — выполнение
    к плану на дату, без темпа; плана нет — блока нет, причина в `passport.planNote`.
  - Сервис (СП-07, `_service`, решение владельца 24.09.2026): оценки клиентов в
    приложении (1–5), негатив по категориям и жалобы ЕГЛ из part_2. Средняя оценка —
    по всем оценкам (`all_rate`), без правил методики — это не официальный уровень
    сервиса; цели в витрине нет — ни цели, ни статуса «выполнен» (появится, когда цель
    придёт в витрине). Неделя: значения по сети, изменения по сопоставимой базе;
    средняя оценка и штуки — разностью (`deltaKind: abs`), число оценок — в %, доля
    негатива — в п. п. Плюс итог с начала месяца (`months`), негатив по категориям,
    ОНПО (слабые — сверху). Качество сервиса — (негатив + жалобы) на 100 тыс. чеков
    (чеки — `cnt_cheq` part_1, по объектам в `_stations`). Зона внимания — АЗС с
    `negativeMin` (2) и больше оценок «1»–«2» за неделю, до `topNegative` (10), с
    главной категорией; `zone_keys` — общий подсчёт зоны для «Главного» и текста ИИ.
    Нет оценок — блока нет, причина в `passport.serviceNote`. Строки part_2 без плана
    (только оценки) плана не создают: `_plan_month` берёт объекты и дни с планом > 0.
  - `narrative.py` — текст ИИ (СП-06): `task()` собирает запрос к Ollama из
    сводки выпуска (с планом), `apply()` сверяет каждое число с моделью
    (`Evidence`: проценты — среди изменений и долей, остальное — среди значений,
    счётчиков, дат, номеров АЗС; `_near` — точность до показанных знаков или
    округление до тыс./млн), запрещает причины и предположения в «Главном» и
    «Внимании», чужие названия в «…»; рекомендации — `recommend.review` (ИИ-16),
    не больше двух, только при непустой зоне внимания, уверенность не выше
    «средней». Любая ошибка — весь текст по шаблону. `recheck()` — повторная
    сверка на сервере: присланному тексту сервер не верит. При смене подсказки
    поднимать `PROMPT_VERSION`.
  - `publish.py` — сборщик на маке. Запуск: сначала очередь (выпуски, которые сайт
    ещё не принял) → спрашивает сервер (`status`: опубликована ли неделя, есть ли
    запрос на перевыпуск) → собирает по витрине ОХД (SQL-таймаут
    `REPORTS_SQL_TIMEOUT`, 300 с) → текст ИИ (две попытки, вторая с temperature
    0,3; модель недоступна — шаблон с причиной) → **копия PDF и Excel в
    `~/Справки АЗС/<неделя>/`** (env `REPORTS_LOCAL_DIR`; помечена «копия с
    компьютера, до публикации») → очередь `~/Справки АЗС/.queue/` → `import`.
    Куда — `REPORTS_IMPORT_URL`: приложение на этом маке (`http://127.0.0.1:8000`,
    `npm start`; так настроено с 24.09.2026) или сайт; не задан — сайт из
    `KPI_IMPORT_URL`. Для локального приложения подсказки об ошибках — «перезапустите
    приложение», для сайта — «bash deploy/deploy.sh».
    Не собралось (нет данных, неполные данные, нет VPN) — `not-formed` с причиной.
  - Копия и очередь (решение владельца 24.09.2026): справка не зависит от сайта.
    Сайт недоступен (нет сети, 5xx, 404 — старая версия без приёма справок, 401/403
    или 503 — токен) — выпуск ждёт в очереди, из ОХД заново не собирается,
    отправляется при следующем запуске; так же развязаны VPN (нужен для ОХД) и сайт
    (из-под VPN может быть закрыт). 400/413/422 — сайт отклонил по существу: выпуск
    уходит в `.queue/rejected/`, повторов нет. `Outbox` — очередь, копии и
    `.state.json` (что собрано и отправлено, о чём уже уведомляли); папка 700,
    одновременно работает один запуск (`.lock`).
  - Уведомления macOS (`osascript`, от «Редактора скриптов»; `REPORTS_NOTIFY=0` —
    выключить): опубликована; собрана, но не ушла; отклонена; копия не сохранилась;
    не собрана — по плановой неделе только после понедельника 13:00 МСК, раньше это
    ожидание данных; сбой сборщика. Каждое — один раз на неделю и причину.
  - `--check` — что мешает справке выйти, без сборки: витрина ОХД (короткий запрос
    за три недели через тот же валидатор), Ollama и модель, сайт и токен, очередь,
    отклонённые, папка копий, расписание launchd (загружено ли, не устарел ли код в
    Application Support, последний запуск). Код 0 — витрина и сайт доступны.
    `--dry-run [--week 2026-W38]` — собрать и показать, ничего не сохранять и не отправлять.
  - `service.py` (сервер): `accept` — проверка модели (`check_model`), повторная
    сверка текста, PDF и Excel, версия; неделя уже опубликована — `exists`, новая
    версия — только по запросу на перевыпуск. `request_reissue` — кнопка
    «Сформировать заново» ставит запрос (`report_requests`), сборщик забирает его в
    ближайший запуск. `not_formed`, `status`. Выпуск, собранный не по витрине
    (`from_mart`: в паспорте нет таблицы `data_for_ai_analytic_part_1` — так
    подписаны старые выпуски по базе KPI), заменяется первым выпуском по витрине без
    запроса (причина `LEGACY_REASON`). Опубликованный выпуск закрывает открытые
    запросы той же недели, поданные до его сборки (`generatedAt`).
  - `store.py` — SQLite `data/reports.sqlite3` (env `REPORTS_DB`), файлы
    `data/reports/` (env `REPORTS_DIR`); выпуск неизменяем, прежняя версия —
    «заменена»; `digest` — отпечаток цифр без времени сборки.
  - `render_pdf.py` / `render_xlsx.py` — PDF (ReportLab, PT Sans по Р-14; Tahoma —
    путями в `REPORT_FONT_REGULAR` / `REPORT_FONT_BOLD`) и Excel с листами «План
    месяца» и «Сервис». Экран, PDF и Excel рисуются из одного JSON и ничего не пересчитывают.
  - `api.py`: `GET /api/reports`, `/api/reports/{id}`, `/api/reports/{id}/file/pdf|xlsx`,
    `POST /api/reports/generate` (админ, запрос на перевыпуск); для мака —
    `POST /api/internal/reports/status|import|not-formed` с токеном
    `REPORTS_IMPORT_TOKEN` (на сервере — `REPORTS_IMPORT_TOKEN_SHA256`). Письмо
    администраторам о несформированной справке — раз в неделю, после 13:00 понедельника.
  - `registry.py` — типы справок; `allowed(user)` — пока только администратор (Р-13).
- Установка на маке: `scripts/install_reports_schedule.sh` — создаёт токен в
  `.env.local`, копирует `backend/` и `data/ai_catalog.dwh.json` в Application
  Support (launchd не читает ~/Downloads), ставит там своё окружение Python 3.10–3.13
  (sqlglot, psycopg2-binary, reportlab и openpyxl — для копий), `AI_CATALOG` и `AI_DB_BACKEND=postgres` задаёт
  сам; launchd каждый час 07:10…21:10. **После правок кода справок установщик
  запускать заново** — он обновляет копию. Журнал — `~/Library/Logs/AZS Classifier/reports.log`.
  Затем `bash deploy/deploy.sh` отвозит SHA-256 токена на сервер.
- Фронтенд: `src/reports.jsx` (`ReportsScreen`), режим `reports` в `main.jsx`, только
  администратор: пункт «Справки» в навигации компьютера и кнопка на главной; на
  нижнюю панель телефона не добавлять. Блок «План месяца» после «Цифр недели»; на
  телефоне «Цифры недели» и план — строками, второстепенные столбцы (`.opt`) скрыты.
  «Сформировать заново» — запрос; плашка «Перевыпуск запрошен» до его выполнения.
- Тесты: синтетическая витрина ОХД `backend/tests/report_stand.py` — те же таблицы и
  столбцы, что part_1/part_2, каталог в диалекте PostgreSQL со схемой dm, валидатор
  с проекцией; проверенный SQL переводится sqlglot в SQLite. Реальные цифры в тесты
  не попадают. Запросы справки проверены и на настоящем каталоге
  `data/ai_catalog.dwh.json` (валидатор пропускает все 12).
- Зависимость на сервере — `reportlab==4.2.5`; на маке для сборки — sqlglot и
  psycopg2-binary, для копий — reportlab и openpyxl.
- Что делать, если справки нет: `./scripts/python.sh -m backend.reports.publish --check`
  называет звено и что сделать. Сайт недоступен — копия уже в `~/Справки АЗС`, выпуск
  уйдёт сам; SSH к серверу нужен только для деплоя, справки идут по HTTPS.
- Дальше: сервис в справке (уровень и качество — из part_2), СП-08 (ОНПО,
  территория, объект — тот же код с другой областью), СП-09 (рассылка).

## Навигация

- `viewItems` несёт три флага: `mobileHidden` убирает пункт с нижней панели
  телефона (так убран «Контроль» — обычному пользователю он не нужен,
  в основной навигации и на главной пункт остаётся), `accent` красит значок
  корпоративным красным, `mark` рисует вместо значка из набора SVG-орб.
  **Сейчас ни `accent`, ни `mark` не стоят ни у кого:** решение владельца от
  22.09.2026 — кнопка «ИИ-аналитик» везде такая же, как на главной (Sparkles,
  нейтральная). Ветки в `ModeSwitcher` и `BottomStrip` сохранены, включаются
  флагом.
- Флаг `optional` — пункт скрыт, пока его не разрешит условие в `navItems`:
  «ИИ-аналитик» — ответ `/api/ai/status`, «Справки» — `isAdmin` (и ещё
  `mobileHidden`: на телефоне справки открываются с главной).
- **Подписи на нижней панели держатся впритык.** Пять колонок делят ширину
  поровну, и самое длинное слово — «Аналитика» — на 390 px укладывается
  с запасом в 1–2 px. Поэтому там выверены зазор сетки (3 px), поля кнопки
  (2 px, на узких телефонах 1 px), минус-трекинг и кегль 10 px ниже 390 px.
  Увеличишь любое из них — слово превратится в «Аналити…». На 320 px оно
  усекается осознанно: ради вымершего телефона подпись пришлось бы увести
  в 8 px на всех остальных. Проверять замером ширины, а не на глаз.

## РУ, ТМ, статус и тип АЗС на витрине ОХД (с 23.09.2026)

В витринах `dm.data_for_ai_analytic_part_1/2` нет привязки объектов к людям, поэтому
вопрос «какой ВД НТУ у Ивановой» раньше давал пустые суммы. Источник — справочники
схемы `bds` (запрос владельца от 23.09.2026):

| Таблица | Что видит модель | Ключ | Как соединять |
|---|---|---|---|
| `bds.l_azs_rm_dt_vers` | `rm_fio` — РУ | `ksss_code` | `rm.ksss_code = f.ksss_azs_code AND f.account_date >= rm.dt_vers_start AND f.account_date <= rm.dt_vers_end` |
| `bds.l_azs_tm_dt_vers` | `tm_fio` — ТМ | `ksss_code` | то же с `tm` |
| `bds.s_azs_ksss` | `status_azs_name`, `type_azs_name` (наличие кафе и магазина — «формат») | `ksss_azs_code` | `k.ksss_azs_code = f.ksss_azs_code`, значения текущие, не на дату |

**Где что лежит**

- `backend/ai/build_dwh_catalog.py` — справочники описаны вручную (`REFERENCES`): только
  эти четыре поля и ключи, правило соединения по периодам (`people_note`), правило поиска
  по фамилии и три примера (фамилии вымышленные). Каталог пересобирается командой
  `scripts/python.sh backend/ai/build_dwh_catalog.py`; `data/ai_catalog.dwh.json` в git не входит.
- `backend/ai/check_dwh_people.py` — проверка справочников на машине с доступом к ОХД:
  типы ключей, пустой конец действующих периодов, пересечения периодов (задвоение сумм),
  покрытие дней последнего месяца, одна ли строка на объект в `s_azs_ksss`, точные
  значения статуса и типа. Печатает только агрегаты, ФИО не выводит. Пишет
  `DWH_outputs/bds_values.json`; сборщик каталога берёт из него значения статуса и типа и,
  если конец периода пустой, переключает соединение на `COALESCE(dt_vers_end, DATE '9999-12-31')`.
- Каталог (`catalog.py`): у таблицы может быть своя схема (`table_schemas`), свой ключ
  области данных (`scope_columns`) и проекция (`projected_tables`).
- Валидатор: таблица из своей схемы без префикса получает его автоматически; схема
  чужая — отказ; справочник всегда подменяется подзапросом с явным списком столбцов
  каталога (для любой роли), для ограниченной роли — ещё и с фильтром по своему ключу
  (`ksss_code IN (…)`). Поэтому `SELECT *` и обращения из CTE не достают из `bds` ничего,
  кроме описанных полей.
- Семантика ОХД: измерения `regional_manager`, `territory_manager`, `station_status`,
  `station_type`, соединения в `entity`, правила про периоды и фамилии; из «чего нет»
  убраны РУ/ТМ и состав сервиса.
- Контракт FAST: на витрине ОХД действуют свои правила (`contract.DWH_RULES`) —
  прежние правила стенда запрещали любые таблицы, кроме двух таблиц SQLite.
- `backend/ai/people.py` — кто назван в вопросе, решает код, а не модель. До обращения к
  модели резолвер берёт всех РУ и ТМ из справочников в пределах области данных
  пользователя (через валидатор; кэш на `AI_PEOPLE_TTL`, 30 мин), находит фамилии в любом
  падеже и применяет правила владельца от 23.09.2026: (1) РУ и ТМ между собой не
  сравнивают — все названные считаются на одном уровне; (2) фамилия есть и среди РУ, и
  среди ТМ — речь о РУ; (3) один только РУ, другой только ТМ — каждый отдельно, без
  сравнения. Модели уходит блок «ЛЮДИ В ВОПРОСЕ» с точными ФИО и фильтром
  `rm_fio IN (…)` / `tm_fio IN (…)` (в FAST — через `generator.generate(context=…)`, в
  агенте — через `hits["people"]`), пользователю — пометка в ответе, кто есть кто. На стенде
  источник — поля `regional_manager` / `territory_manager` таблицы `stations`. Те же правила
  записаны в справочник сокращений (`contract.GLOSSARY`), в описание каталога ОХД и в
  семантику — на случай, когда резолвер фамилию не нашёл.
- Проекция действует и для витрин `dm`: из таблицы выходят только столбцы каталога,
  нужные запросу (`validator._projection`), поэтому `rating` и `dt_ins` недоступны даже
  через `SELECT *` или CTE.

**Правила, которые нельзя ослаблять**

1. Новые поля справочников — только правкой `REFERENCES` в сборщике, по решению владельца.
   Модель не должна видеть ни телефонов, ни других столбцов этих таблиц.
2. Итог по руководителю считается по дням закрепления: объект, сменивший ТМ в середине
   месяца, делится между двумя ТМ. Если проверка покажет пересечения периодов — сначала
   правило выбора периода, потом ответы по людям.
3. Фамилия ищется в обоих справочниках (UNION ALL `rm_fio` и `tm_fio`) по основе без
   окончания через `ILIKE`; в ответе — полное ФИО, роль и число АЗС.

## Область данных ИИ на витрине ОХД (с 23.09.2026)

Решение владельца: ИИ живёт в ДВХ и всё считает в нём. При `AI_DB_BACKEND=postgres`
список объектов пользователя вычисляется запросом к ОХД, а не по `stations.json`
(`backend/ai/dwh_scope.py`, вызывается из `scope.build`; этим же путём идут плитки сводки):

| Роль | Откуда объекты |
|---|---|
| РУ / ТМ | `bds.l_azs_rm_dt_vers` / `bds.l_azs_tm_dt_vers`: закреплённые за ФИО из привязки на сегодня |
| АУП общества | `dm.data_for_ai_analytic_part_1`: объекты с этим `npo` за `AI_SCOPE_NPO_DAYS` дней (60) |
| Управляющий, агент | КССС из привязки как есть |
| Вся сеть | без запросов |

- Запросы системные, значение привязки экранируется; результат кэшируется на `AI_SCOPE_TTL` (30 мин).
- Витрина не ответила — область пустая с причиной в подписи, не «вся сеть»; ошибка не кэшируется.
- Режим «от имени» и самопроверка изоляции (`selfcheck`) берут РУ/ТМ/ОНПО тоже из ОХД.
- Стенд SQLite не изменился: там область по справочнику приложения (`roles.resolve`).
- Аварийный выключатель: `AI_SCOPE_SOURCE=reference` возвращает прежний способ.
- В каталог добавлен пример «Кто РУ и ТМ у АЗС 5044» (РУ и ТМ на сегодня, объект по КССС или номеру).

**Известное расхождение (З-29, бэклог).** Экраны и карточка АЗС по-прежнему работают по
`stations.json`: один и тот же пользователь может видеть на экране и в ИИ разный набор
АЗС, а ФИО в привязке учётной записи может не совпасть с написанием в ОХД (тогда область ИИ
пустая с пометкой «в ОХД нет объектов по этой привязке»). На время пилота допустимо.

## Исполнитель SQL: стенд и витрина

`AI_DB_BACKEND=sqlite` — демонстрационный стенд, `postgres` — витрина в ОХД.
Переключение меняет не только источник, но и **типы значений**, и это
единственное, что стенд проверить не может.

- **Всё, что приходит из драйвера, проходит через `_normalize`** в
  `backend/ai/executor.py`. psycopg2 отдаёт NUMERIC как `Decimal`, даты —
  как `date`/`datetime`, плюс UUID и `memoryview`; дальше строки уходят
  в `json.dumps` — в поток ответа, в журнал, в диалог и в выгрузку. Без
  приведения первый же вопрос про выручку падает с «Object of type Decimal
  is not JSON serializable». Приводим на границе исполнителя, а не
  в потребителях: потребителей четыре, и один обязательно забудут.
- **Целевые типы — те же, что даёт SQLite**, чтобы один вопрос давал
  одинаковый ответ на стенде и на витрине: целочисленный `Decimal` → `int`,
  дробный → `float`, дата → строка ISO.
- **NaN и Infinity превращаются в `null`.** `json.dumps` пишет их голыми
  литералами `NaN`/`Infinity` — это невалидный JSON, и `JSON.parse`
  в браузере падает на всём ответе. В Postgres NaN даёт и float8, и
  NUMERIC (например, деление на ноль в плане).
- **Целое за 2^53 уходит строкой.** В JavaScript такое число молча
  округлится, и человек увидит неверную цифру, не поняв этого.
- **Проверки стенда не ловят ошибки типов витрины.** Эталонный набор
  и `selfcheck` гоняются по SQLite, где `Decimal` не существует. Всё, что
  касается типов, закрывается тестами `backend/tests/test_ai_executor_types.py`
  на подменённом исполнителе, а не прогоном по стенду.

## Python окружения

Системный `python3` на машине владельца — Homebrew 3.14, там зависимостей
бэкенда нет (под 3.14 нет сборок `pandas` и `psycopg2`). Рабочее окружение —
`.venv` на 3.12. Всё, что запускает бэкенд, ходит через `scripts/python.sh`;
голый `python3` допустим только у `npm start` (`scripts/run_app.py`), который
это окружение и собирает. Новый скрипт с `python3 -m uvicorn` внутри — это
готовая ошибка «No module named uvicorn».

## Администрирование

Три вкладки (`ADMIN_TABS`): пользовательская аналитика, перечень
пользователей, качество ответов ИИ. Последняя появляется только при
включённом контуре ИИ; если контур выключили, а вкладка выбрана, она
переключается на аналитику сама. Переключатель периода показывается
только на вкладке аналитики — у качества свой.

## Раздел «Качество ответов»

Панель в админке (`AiQualityPanel`), данные — `backend/ai/quality.py`.

- Отказ (граница области данных сработала) и техническая ошибка (сломался
  контур) считаются **раздельно**. Смешивать нельзя: лечатся по-разному.
- Дата первичной реакции ставится один раз, при первом переводе из «Новой».
  Иначе срок можно «обновить» повторным нажатием, и просроченных не станет.
- Версия системной инструкции считается из её текста
  (`contract.prompt_version()`), а не ведётся руками, и пишется в журнал.
  Без этого срез «до и после смены версии» (БТ-КК7) не строится.
- Переписки в разделе и в выгрузке нет — только метаданные, оценка,
  комментарий и текст запроса к витрине (модель приватности ИБ-4, БТ-КК8).
  Выгрузка не должна давать больше, чем экран.
- Надпись про БТ-КК10 в интерфейсе обязательна: как только оценка попадёт
  в разговор о работе человека, низкие оценки исчезнут, и продукт перестанет
  получать сигнал о том, где врёт. С 21.09.2026 она не отдельный блок,
  а последняя фраза подписи под лентой оценок — владелец просил убрать
  блок. Совсем убирать фразу нельзя без правки самого БТ-КК10.

- **Следующий шаг:** область полномочий субадминистратора (БТ-КК1) и
  классификатор вопросов для фильтра по классу (БТ-КК4).
