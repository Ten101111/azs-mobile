"""Аналитический агент: модель-оркестратор над надёжными инструментами.

Точка входа — `loop.run`. Всё, что агент может сделать с данными, перечислено
в `tools.py`; каждый SQL проходит тот же валидатор и исполнитель, что и
прежний контур «вопрос → SQL → ответ» (он сохранён как режим FAST).
"""
